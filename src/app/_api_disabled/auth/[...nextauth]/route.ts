import { NextResponse } from "next/server";

// Placeholder until NextAuth is configured in Task 03
export async function GET() {
  return NextResponse.json({ message: "Auth not configured — see Task 03" });
}
export async function POST() {
  return NextResponse.json({ message: "Auth not configured — see Task 03" });
}
