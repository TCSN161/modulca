# Session: Testing — TEST-001 — 2026-04-23

> **Role**: Testing Specialist
> **Task ID**: TEST-001
> **Pre-dispatch-created**: 2026-04-20 by Weekly Ops WEEK-2026-W17 (eliminates Tue-AM friction)
> **Expected spawn**: 2026-04-23 (Thursday) or 2026-04-24 (Friday) EET — after FE-QA-001 closes (cap respect)
> **Expected close**: same day or 2026-04-24 (Friday) EET
> **Orchestrator**: Weekly Ops WEEK-2026-W17 (`claude/nice-chandrasekhar-aa2808`)
> **Captain**: CAPTAIN-2026-04 (`claude/modest-blackwell-dfbf1f`)
> **Dependency**: FE-QA-001 must be closed BEFORE this spawns (cap respect + avoids overlap on component files)

---

## 📋 SPAWN PROMPT — paste into a NEW Claude Code chat as first message

```
===== BEGIN SPAWN PROMPT =====

ROLE: Testing Chat (Specialist — 3-Layer Captain Architecture)
TASK ID: TEST-001
SPAWN TIME: 2026-04-23 or 2026-04-24 [HH:MM] EET
CAPTAIN CHAT: CAPTAIN-2026-04
ORCHESTRATOR: WEEK-2026-W17 (Weekly Ops)
SIBLING CHATS: Captain (strategic) + Weekly Ops (sprint exec) active. FE-QA-001 closed (cap respect confirmed). EXT-OPS-001 dormant. Cap 3/3 momentary with this spawn.

SCOPE: Test coverage pre-launch — Playwright E2E for auth flow + Stripe checkout (MOCK MODE — live cutover is post-bank-verification, NOT in scope) + CloudSyncProvider unit coverage gap. Write tests only; DO NOT modify application code.

WHY NOW (beta launch T-8 or T-7 days):
- Regression moat before first real users hit auth + paywall flows.
- Must run green for 3 consecutive days before May 1 launch (3-day rule per Captain).
- Stripe checkout mock is safe this week; real live keys come after bank verification clears.

ALLOWED PATHS:
  READ: full repo (tests need to understand what they test)
  WRITE:
  - src/**/__tests__/** (unit tests colocated in feature folders)
  - src/**/*.test.ts and src/**/*.test.tsx (colocated tests)
  - tests/e2e/** (Playwright E2E — create folder if not exists)
  - playwright.config.ts (ONLY if user approves a dep addition)
  - vitest.config.ts (ONLY if user approves)
  - docs/sessions/active/2026-04-23-testing-TEST-001.md (THIS FILE — you own it from spawn)
  - docs/sessions/TRACKER.md (status)
  - docs/TASK_MASTER.md (mark E1/E2/E3/E4 Status = Done as you close)

FORBIDDEN:
  - src/app/** non-test files (application code — read-only)
  - src/features/** non-test files (application code — read-only)
  - scripts/, supabase/, next.config.*, .env*
  - package.json unless user explicitly approves a test-dep addition
  - Spawning a 4th chat

READ FIRST at spawn (~15 min):
  1. docs/META_SYSTEM.md §3-Layer + §Golden rules
  2. docs/sessions/README.md
  3. docs/PARALLEL_SESSIONS.md §2
  4. docs/sessions/TRACKER.md
  5. THIS scratchpad (continuation notes from Weekly Ops)
  6. docs/sessions/active/2026-W17-weekly-ops.md
  7. docs/sessions/active/2026-04-21-frontend-qa-FE-QA-001.md (FE-QA findings; some may need regression tests)
  8. docs/audits/2026-04-17.md §1 Technical (test coverage gaps)
  9. Existing test files to learn the style: src/features/auth/store.test.ts, src/shared/lib/*.test.ts
  10. src/ARCHITECTURE.ts (project map)

PRIORITY STACK FOR THIS SESSION (in order):

Task 1 — Playwright E2E: Auth flow (estimated 1.5-2h)
  Scenarios (no existing E2E):
  1. Email signup → confirm email sent → simulated click → Dashboard (happy path)
  2. Returning login → session persists → reload page → still logged in
  3. Sign out → redirected to home → auth cookies cleared → can't access /app/* without login
  4. Protected route: unauthenticated user hits /architect → redirect to /auth with return-to preserved
  5. Demo mode fallback (no Supabase env): localStorage-backed auth works end-to-end

Task 2 — Playwright E2E: Stripe checkout MOCK mode (estimated 1-1.5h)
  Scenarios (mock the Stripe client; do NOT hit real Stripe):
  1. Guest → Pricing → Click "Upgrade to Creator" → Stripe checkout opens (mock) → Success webhook fires → User tier updated in DB → Dashboard reflects new tier
  2. Webhook signature validation: correct signature passes, wrong signature 400s
  3. Idempotency: same webhook delivered twice → DB mutation once (not duplicated)
  4. Tier downgrade webhook: paid → canceled → tier reverts → user sees appropriate CTAs

Task 3 — Unit tests: CloudSyncProvider coverage gap (estimated 1h)
  Per audit §1, CloudSyncProvider has untested hydration + migration + debounced-save paths.
  Targets:
  - Hydration from Supabase on mount; fallback to localStorage if offline
  - Migration from legacy localStorage schema to current
  - Debounced save (no duplicate writes within 500ms window)
  - Edge cases: empty state, corrupt localStorage, network timeout mid-save

OPERATING RULES:
- Follow existing test style (vi.mock with vi.hoisted for mocks referenced in factories)
- Tests must be fast: <5s per test, <30s per file
- Use beforeEach/afterEach for setup/teardown; no test-order dependencies
- NO flaky tests — if timing-dependent, use explicit waits not setTimeout
- After each new test: `npx vitest run <file>` — then `npx vitest run` full suite before commit
- Commits: `test(<scope>): <what>` (scopes: `e2e-auth`, `e2e-stripe`, `unit-cloudsync`, `unit-webhook`)
- NEVER commit with bare `git commit` — always explicit paths per CLAUDE.md

KILL SWITCH:
- If Playwright isn't installed and requires `npm install`: STOP, escalate to Weekly Ops for approval (package.json change is user decision per Captain brief)
- If mock setup bleeds into production code: STOP, refactor out of tests/ boundary
- If a test discovers a real bug (not a test bug): log in this scratchpad + consider bug-fix Specialist next week — do NOT fix inline (that's outside testing scope)

SESSION OUTPUT (append to this file):
  ## Tests added (file + scenario count)
  ## Coverage delta (before/after % if measurable via vitest --coverage)
  ## Bugs discovered during test writing (logged for bug-fix Specialist)
  ## E2E scenarios automated (green count)
  ## Handoff to Weekly Ops

On close:
  1. Run `npm run typecheck` + `npm run test:run` — all green
  2. Run Playwright once standalone (`npx playwright test`) — all green
  3. Update TRACKER.md: TEST-001 row → Past
  4. Update TASK_MASTER.md: mark E1/E2/E3/E4 Status = Done for closed items
  5. Commit: `chore(session): close TEST-001 — <outcome>`
  6. Push to master
  7. Append "Handoff to Weekly Ops" section below

===== END SPAWN PROMPT =====
```

---

## Context from Weekly Ops (prepared 2026-04-20)

### Why Thursday/Friday, not earlier

FE-QA-001 runs Tue-Thu. Cap respect: FE-QA-001 must close before TEST-001 spawns (Captain + Weekly Ops + FE-QA-001 = 3/3; adding TEST-001 before FE-QA-001 closes = 4/4 breach).

### Stripe live cutover is NOT in scope

Explicit from Captain brief: Stripe go-live depends on bank verification (see TRACKER blockers). TEST-001 uses MOCK mode only. Real Stripe live cutover becomes OPS-NNN Specialist next week post-bank-verification — DO NOT touch live Stripe keys from this session.

### Dependencies before spawn

- FE-QA-001 closed (confirmed by TRACKER row move to Past)
- Weekly Ops has pulled FE-QA-001 bug queue into `docs/sessions/active/2026-W17-weekly-ops.md` (some FE-QA bugs may need regression tests here)

### Handoff notes

- Your close triggers Weekly Ops to write Friday Handoff to Captain + close WEEK-2026-W17.
- If any E2E test needs `npm install` of a new dep (e.g., `msw` for network mocking), STOP and escalate — package.json change requires user decision.
- Seed data for E2E: use existing demo accounts or Supabase test users. Do NOT create Stripe live-mode test charges.

---

## 🗂️ Specialist-owned sections (you fill these as you work)

### Tests added (file + scenario count)

_(list as you write — include test file path, number of `it()` blocks, assertions summary)_

### Coverage delta

_(before/after if `npm run test:coverage` available)_

### Bugs discovered during test writing

_(log here — these go to bug-fix Specialist next week, NOT fixed inline in this session)_

### E2E scenarios automated

- [ ] Auth 1: Email signup → Dashboard
- [ ] Auth 2: Login persists on reload
- [ ] Auth 3: Sign out clears session
- [ ] Auth 4: Protected route redirect
- [ ] Auth 5: Demo mode fallback
- [ ] Stripe 1: Checkout mock + webhook + tier update
- [ ] Stripe 2: Webhook signature validation
- [ ] Stripe 3: Idempotency (duplicate webhook)
- [ ] Stripe 4: Tier downgrade on cancel

---

## Handoff to Weekly Ops

_(write on close — Weekly Ops reads this for Friday Handoff to Captain)_

### Outcome summary
_TBD_

### What shipped (commits)
_TBD_

### What's queued (bugs found → bug-fix Specialist next week)
_TBD_

### Coverage improvement
_TBD_

### Green run status
_TBD_

### Ready for launch (May 1)?
Yes / Yes-with-caveats / No (with list)
